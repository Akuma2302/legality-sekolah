import { STATES } from './school.model.js';

/** Teachers reuse the same Malaysian state list as schools. */
export { STATES };

export const TEACHER_FIELDS = ['name', 'position', 'subject', 'phone', 'state'];
